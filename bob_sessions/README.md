# Bob task session reports

Required submission deliverable. Contains, for every Bob task used to build this project:

- the exported task history markdown file
- a screenshot of the task session consumption summary

Export path in Bob IDE: chat panel > Views and More Actions > History > select task > click task header > screenshot the consumption summary > Export task history icon.
